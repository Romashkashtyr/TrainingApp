<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <uses-permission android:name="android.permission.BODY_SENSORS"/>
    <uses-permission android:name="android.permission.BODY_SENSORS_BACKGROUND"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:name=".TrainingApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrainingApp"
        tools:targetApi="31"
        tools:ignore="ForegroundServicePermission">
        <activity
            android:name="com.example.splash.ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.TrainingApp" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.trainings.ui.training_activity.TrainingsListActivity"
            android:exported="false" />
        <activity
            android:name="com.example.auth.ui.AuthorizationActivity"
            android:exported="false"
            android:theme="@style/Theme.TrainingApp" >
        </activity>
        <activity
            android:name="com.example.main.ui.MainActivity"
            android:exported="false" >
        </activity>
        <service
            android:name="com.example.main.service.StepsCounterService"
            android:foregroundServiceType="health"
            android:exported="false"/>
    </application>

</manifest>